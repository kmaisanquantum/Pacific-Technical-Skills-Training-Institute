# PostgreSQL Connection String (Coolify will inject this automatically if using Postgres service)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ptsti

# Anthropic API Key for the AI Tutor (Server-Side Proxy)
ANTHROPIC_API_KEY=sk-ant-placeholder

# Monolithic Application Port
PORT=5000

# Node Environment
NODE_ENV=production
